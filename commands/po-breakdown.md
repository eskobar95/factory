# /po-breakdown (deprecated)

**Renamed to `/to-plan`.** Same behavior: macro idea → PRD + TECHSPEC + milestones + tasks.

Use the planning track instead:

| Old | New |
|-----|-----|
| `/po-breakdown` | `/to-plan` (all-in-one) |
| — | `/align` → `/to-prd` → `/to-backlog` (recommended) |

See [commands/planning/to-plan.md](planning/to-plan.md).
