{
  "description": "Run npm/pnpm audit when package.json or lockfile changes. Appends summary to .ai/logs/diary.md.",
  "hook": "afterFileEdit",
  "entry": {
    "command": ".cursor/hooks/run-security-audit.sh",
    "failClosed": false,
    "timeout": 180
  }
}
